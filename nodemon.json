{
  "watch": ["server", "build-time-constants", "build-tools"],
  "ignore": ["build-time-constants/build.d.ts"],
  "exec": "ts-node --project server/tsconfig.json --files server/index.ts",
  "ext": "js ts",
  "env": {
    "NODE_OPTIONS": "--inspect"
  }
}
